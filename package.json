{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@redwoodjs/cli-storybook": "6.0.6",
    "@redwoodjs/core": "6.0.6"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": "=18.x",
    "yarn": "3.x"
  },
  "prisma": {
    "seed": "yarn rw exec seed"
  },
  "scripts": {
    "build": "yarn install && yarn rw build && yarn rw prisma migrate deploy",
    "update": "git pull && yarn build && yarn start",
    "serve": "yarn rw dev",
    "start": "yarn rw serve",
    "storybook": "yarn rw storybook"
  },
  "version": "2.2.0",
  "packageManager": "yarn@3.6.1"
}
