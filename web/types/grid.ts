export type GridConfig = {
  rows: number
  columns: number
  pixels: number
}

export type Selection = {
  row: number
  col: number
}

export type GridData = {
  row: number
  col: number
  label: string
}
